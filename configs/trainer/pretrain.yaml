accelerator: gpu
devices: 1
strategy: auto
precision: bf16-mixed
max_steps: 200000 # ~ 50 epochs on XCL
grad_clip: 1.0
log_every_n_steps: 200
save_every_n_steps: 20000
resume_from_checkpoint: null #/home/moritz_earthspecies_org/soundscape_mae/checkpoints/step_0120000.ckpt
